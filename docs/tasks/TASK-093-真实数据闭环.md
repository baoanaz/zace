# TASK-093：真实使用数据采集闭环（TASK-023 + TASK-050 的合并落地）

> 状态：pending（依赖已解除，可开工）｜ 阶段：Phase 2（M2b）｜ 硬依赖：TASK-084（✅ 已合并）、TASK-091（✅ 已合并）
> 建议分支：`feature/task-093-telemetry_<你的缩写><MMDD>`
> 交付物所有权：
> - `service/zace_service/metadb.py`（审计表的**读取侧**聚合，如需）
> - `service/zace_service/routers/ops.py`（如需新增只读导出端点）
> - `benches/`（把真实查询转成候选用例的脚本）
> - `docs/handbook/质量数据.md`（**新建**）
> - `docs/tasks/TASK-023-真实场景用例采集.md`（回填）
>
> 清单外文件不得改。**本卡不调任何检索参数（R29/R30 冻结）；调参是 TASK-050，需用户单独授权。**

## 背景（用户 2026-09-14）

> 明天我仔细打磨两个工具的返回内容，以及补全多个真实的问题，打磨 benchmark 的设计。
> 做好 tool 和质量之后，就可以发布给其他人使用，进行压测了。

**现状**（编排者核实）：

| 项 | 现状 |
|---|---|
| `query_audit` 表 | **已有**（TASK-084），记录 `query`/`answerable`/`confidence`/`latency_ms`/`evidence_json`（**只有元数据，无源码**） |
| TASK-023（真实场景采集） | `pending`，未开工 |
| TASK-050（质量调优） | 未开卡，**必须基于 TASK-023 的真实数据** |
| 当前质量参数 | **冻结**（R29/R30）：`docs_ratio` / `rerank` 权重是 smoke 集拟合值，**未经真实数据校准** |

**关键闭环**：`query_audit` 已经在被动积累真实查询 → 但它们**没有被用来改进质量**。
本卡把这条链路打通：**真实查询 → 候选用例 → 标尺扩充 → 质量优化有据可依**。

## §A 数据采集（已有基础，本卡补齐）

- `query_audit` 已在记录（TASK-084）；
- 本卡要补的是：**"这次检索好不好"的反馈信号**。可选（**你评估，报告说明**）：
  - 显式：用户在 WebUI 上对某次查询点"有用/没用"（需前端改动，**可能要另开卡**）；
  - 隐式：Agent 是否在拿到结果后**追加了一次改写查询**（审计里可看出同 session 的连续查询）；
  - 半自动：把 `answerable=false` 且用户紧接着改问的**成对查询**标为"首问失败"。
- **隐私纪律**：只存查询文本与元数据，**绝不存源码内容**（Module/04 §8 已定）。

## §B 从真实数据到用例（本卡的核心产出）

写一个**离线脚本**（放 `benches/`），把 `query_audit` 导出成 golden 候选：

- 输入：`zace-meta.db` 的 `query_audit` 表 + 人工标注；
- 输出：`benches/golden/<repo>/candidates.jsonl`（**候选**，非最终用例）；
- **人工把关**：候选必须经人核验（`expected` 要 grep 过）才能进正式 golden 集
  —— 这是 TASK-091 的出题纪律，**不得自动生成就入库**；
- 报告要给出：**候选数量、真实分布（lang/category）、与现有用例的重叠度**。

## §C 质量调优的准备工作（**不做调参**）

- 输出一份 `docs/handbook/质量数据.md`，写清：
  1. 数据从哪来（`query_audit` 字段表）；
  2. 如何导出（脚本命令）；
  3. 如何扩充 golden（人工核验流程）；
  4. **哪些参数可以在数据充分后调整**（列出 R29/R30 冻结的那批，以及各自的调整依赖）；
  5. **什么条件下可以解除冻结**（如"每 category ≥20 条真实用例"），交用户拍板。
- **TASK-050（真正的调参）不在本卡范围**：本卡只交付"数据与流程"，
  用户明确说"等架构稳定后"+ 需要真实数据支撑才动手。

## 验收标准（DoD）

- [ ] 导出脚本可运行，**贴真实输出**（从现有 `query_audit` 导出 N 条候选）。
- [ ] 候选到正式用例的**人工核验流程**写进手册，且**至少完成 5 条**完整示范
      （从真实查询 → grep 核验 → 入库）。
- [ ] `docs/handbook/质量数据.md` 完整（含"何时可解冻参数"的判据）。
- [ ] **反馈信号**：若做了显式反馈（前端），贴 UI 与落库验证；若判断应另开卡，写进未决问题。
- [ ] 隐私检查：导出文件里**不含源码内容**（grep 验证）；贴证据。
- [ ] 基线三条命令全绿（用 `-o addopts=""` 看数字）。
- [ ] 任务卡"执行记录"已回填；任务板 TASK-023 行状态改为 `review`（**本卡是它的落地**）。

## 明确不做

- **不做参数调优**（TASK-050，需用户单独授权 + 数据充分）；
- 不改检索/装填/rerank 的任何实现或默认值（R29/R30 冻结）；
- 不自动生成 golden 用例（必须人工核验）；
- 不做用户行为分析平台（V1 只需要能导出候选）。

## 完成报告（回填）

按 `docs/plan/orchestration.md` §3 模板。**必须包含**：真实候选数量与分布、
5 条完整示范（真实查询 → 核验 → 入库）、以及"何时可解冻参数"的建议判据。

## 执行记录

（实施 AI 在此填写。）
