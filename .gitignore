# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.uv/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# --- Rust (client/) ---
target/

# --- Node (web/) ---
node_modules/
web/dist/
.pnpm-store/

# --- zace 运行时产物（本地调试可能落在仓库内）---
.zace/
data/
*.db-wal
*.db-shm

# --- 密钥与本地配置（W6：云端 embedding key 绝不入库）---
.env
.env.*
!.env.example

# --- 编辑器 / 系统 ---
.idea/
.vscode/
.DS_Store

# --- Rust 构建产物（client/）已在上方覆盖；此处补 examples/ 的临时产物 ---
client/target/

# --- npm 分发（TASK-052）---
npm/node_modules/
npm/*.tgz

# npm 平台子包的二进制**不进 git**（D-48）：由 CI 在发布前从构建产物 stage 进来。
# 只提交 `npm/platforms/*/package.json` 与 `.gitkeep`。
# 为何必须排除：这些是几 MB 的 ELF/Mach-O/PE 文件，进 git 会让仓库失控，
# 且**本机编的产物与 CI 编的不同 flavour**（实测：本地 gnu 动态 / CI musl 静态），
# 一旦提交就可能被误当成发布产物。
npm/platforms/*/zace-client
npm/platforms/*/zace-client.exe
npm/platforms/*/*.tgz
